# LDraw Parts Library mirror (huge, regenerable from pybricks/ldraw git clone)
corpus/ldraw/

# Note: MPDs ARE committed to the repo (~250 MB total).
# They can be regenerated with:
#   cat corpus/setlist_<cohort>.txt | xargs -I {} -P 24 sh -c \
#     'curl -sL --max-time 60 -o corpus/mpds_<cohort>/$(basename {}) {}'


# Python caches
__pycache__/
*.pyc
*.pyo

# Outputs and screenshots (regenerable)
output/dashboard_preview*.png

# IDE / editor
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
